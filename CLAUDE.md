# CLAUDE.md — analysis_of_estate 実装ガイド

このリポジトリは、不動産賃貸物件の空室情報をスクレイピングで収集し、時系列データとして蓄積・分析し、希望条件に合致する物件を通知するアプリの設計・実装リポジトリです。

## 文書体系

| 文書 | 役割 |
|---|---|
| `不動産スクレイピングアプリ設計.md` | 基本設計書。アーキテクチャ・法的リスク管理・DBスキーマ（SCD Type 2）・通知設計・分析メトリクスの根拠。実装はこの内容と乖離してはならない（`.claude/rules/design-doc-sync.md`）。 |
| `.claude/rules/` | 実装時に従う運用ルール（コンプライアンス・TDD・設計書同期・判断依頼の書式） |
| `.claude/agents/` | `estate-builder`（実装者）・`estate-reviewer`（レビュー役） |
| `.claude/skills/compliance-review-loop/` | 実装者とレビュー役を往復させるオーケストレーション手順 |

設計書は「現時点で最良の作業仮説」である。実装を進める中で矛盾や陳腐化に気づいた場合、無断で書き換えず、ユーザーに報告して合意を得ること。判断に迷う場合は常に安全側（法的リスク・データ破損リスクが小さくなる側）を選ぶ。

## ハードルール（機械的に守る事項）

設計書「コンプライアンスおよびスクレイピングにおける法的リスク管理」に基づく。

1. スクレイパーには必ずレート制限・指数バックオフ・同時実行数制限を実装する（`.claude/rules/scraping-compliance.md`）。
2. ログイン認証が必要な領域のスクレイピング、CAPTCHA等の技術的制限の意図的な迂回は行わない。
3. 収集対象は物件という「モノ」のデータ（価格・面積・所在地・築年数・物件種別等）に限定し、個人情報・要配慮個人情報は収集しない。
4. 物件画像・独創的な説明文の複製・再公開は行わない（事実データの抽出に限定する）。
5. 空室履歴テーブルへの書込は追記型（SCD Type 2: `valid_from` / `valid_to` / `is_current_record`）とし、過去レコードの上書き・削除は行わない。
6. 通知ロジックは「新規に `is_current_record = TRUE` となった、かつ直前が募集中でなかった」物件のみをトリガーとし、重複通知を防ぐ。
7. シークレット（APIキー・認証情報）をコード・設定・ログに書かない。

## 実装フロー

1. `estate-builder` が設計書に基づき実装・テストを行う（`src/` 配下は TDD 必須。`.claude/rules/tdd-python-testing.md`）。
2. スクレイパー/クローラー/通知/DBスキーマに変更が入る場合は、完了とみなす前に `estate-reviewer` のレビューを経る。`.claude/skills/compliance-review-loop` で一括実行できる。
3. 設計書との乖離、法務・レート制限とのトレードオフ等でユーザーに判断を仰ぐ場合は `.claude/rules/pr-judgment-request.md` の書式に従う。

## コマンド

（実装が進み次第、テスト実行コマンド等をここに追記する）
