# 不動産スクレイピングアプリ 基本設計書

| 項目 | 内容 |
|---|---|
| 版 | 0.4 |
| 作成日 | 2026-09-05 |
| 状態 | **ドラフト**。第8章「未確定事項」が解決するまで `src/` の実装に着手しない |
| 位置づけ | 本リポジトリの SSoT（`CLAUDE.md` および `.claude/rules/design-doc-sync.md` に定める通り） |

## 0. この文書について

### 0.1 素材と、素材を SSoT としなかった理由

本設計書は `gemini_reports_about_estate_scraping.md`（AI 生成の調査レポート、以下「素材レポート」）を出発点とするが、**素材レポートは SSoT ではない**。`estate-reviewer` によるレビューで `VERDICT: BLOCK` と判定されたためである。判定理由の要旨は次の3点。

1. 対象サイトの利用規約について、**出典と矛盾する事実主張**が含まれていた（詳細は附録A-1）。
2. `CLAUDE.md` ハードルール2（技術的制限の意図的迂回の禁止）と**正面から衝突する節**が含まれていた（附録A-2）。
3. UR 公式 `robots.txt` が実際にクロール対象エンドポイントを Disallow している事実が**完全に欠落**していた（附録A-3）。

加えて、素材レポートが示した SCD Type 2 のサンプルスキーマはデータモデルとして成立していなかった（附録A-4）。

素材レポートは参考資料として残置する。本文書と矛盾する場合は**常に本文書が優先する**。

### 0.2 記述の原則

- **具体値を書く。** 「人間のブラウジングと同等」のような定量化されていない表現を残さない。実装者が推測で埋めた値が、本システム最大の法的リスクの根拠になるため。
- **出典は一次情報に限る。** 法令の条文、対象サイトの実測、公式ドキュメント。ベンダーのマーケティング記事・個人ブログ・Reddit・文書共有サイトを設計判断の根拠にしない。
- **検証していないことは「未検証」と書く。** 第8章に隔離する。

---

## 1. スコープと前提

### 1.1 目的

**UR賃貸住宅において、運用者本人の希望条件に合致する部屋の新規募集を即時に検知し、Discord へ通知する。**

本システムはこれ以外の目的を持たない。収集した空室情報は時系列履歴として蓄積されるが、それは**通知の重複排除に状態遷移の履歴が必要だから**であり（第4章冒頭）、市場分析を目的とするものではない。

### 1.2 スコープ

| 範囲 | 内容 |
|---|---|
| **対象サイト** | ① UR賃貸住宅（`www.ur-net.go.jp`）／② JKK東京＝東京都住宅供給公社（`www.to-kousya.or.jp` および `jhomes.to-kousya.or.jp`） |
| **機能** | 希望エリア限定の差分クロール → SCD Type 2 での状態蓄積 → 新規募集の Discord 通知 |

いずれも公的な住宅供給主体（UR＝独立行政法人都市再生機構、JKK東京＝東京都が全額出資する特別法人）であり、民間ポータルと比べて掲載データの性質が近い。ただし**両サイトの技術的性質は大きく異なり、リスクプロファイルも同一ではない**（第2.2節・第3.4節）。

> **サイトは2つだが、robots.txt とレート制限の単位は「ホスト」である。** JKK東京は案内サイト（`www.to-kousya.or.jp`）と空室検索システム（`jhomes.to-kousya.or.jp`）で**ホストが分かれている**ため、本システムが相手にするホストは合計3つになる。第3.2節・第3.3節はホスト単位で適用する。

### 1.3 スコープ外（明示的に作らないもの）

以下は本設計書の対象外である。将来的に検討する場合は、**設計書の追記ではなく、第2章のリスク評価のやり直しから始める。**

| スコープ外 | 理由 |
|---|---|
| **SUUMO 等の民間募集ポータル** | 掲載にダミー物件（実在しない／既に成約済みの「おとり物件」）が含まれ、時系列データとしての信頼性が担保できない。加えて利用規約で自動収集を明示的に禁止していることが多く、法的リスクの水準が公的主体と大きく異なる |
| **都営住宅（東京都住宅政策本部）等、上記2サイト以外の公的賃貸** | 対象を2サイトに留め、それぞれの巡回経路とレート設計を実測に基づいて確定させるため。追加する場合は第2章のリスク評価を対象サイトごとに実施する |
| **JKK東京の会員機能（JKKねっと マイページ・ユーザ登録・住宅の申込）** | ログインを要する領域。不正アクセス禁止法に抵触しうるため一切アクセスしない（第2.4節） |
| **不動産投資のための市場分析**（稼働空室率・DOM・賃料トレンド・機械学習等） | 運用者の判断によりスコープアウト。これにより著作権法第30条の4 但書（有償データベース市場の侵食）に関する懸念がほぼ解消し、本システムは「個人が自分の転居先を探すための空室ウォッチャー」という最も擁護しやすい位置づけになる |
| **収集データ・分析結果の第三者提供／公開／販売** | 第2.5節 |

> **分析のスコープアウトが設計に与えた影響:** 素材レポートには「通知は狭いエリアを高頻度」「分析は広いエリアを低頻度」という**要求の対立**が潜在していた（レビュー所見）。分析を外したことでこの対立が消え、**希望エリア限定の差分クロールで確定**できるようになった。クロール範囲が1〜2桁小さくなり、対象サーバーへの負荷と法的リスクの両方が下がる。

### 1.4 前提条件

| 項目 | 内容 |
|---|---|
| 対象サイト | UR賃貸住宅 / JKK東京（計3ホスト — 第1.2節） |
| 収集範囲 | **運用者が指定した希望エリアに限定した差分クロール。**全域クロールは行わない |
| 利用者 | 運用者本人のみ。第三者への機能提供・データ提供は行わない（第2.5節） |
| 実行環境 | 自宅 Raspberry Pi による Kubernetes クラスタ（ARM64） |
| 通知先 | Discord Webhook |
| データベース | PostgreSQL 17（拡張なし。TimescaleDB は採用しない — 第7章） |

### 1.5 用語

| 用語 | 意味 |
|---|---|
| **SCD Type 2** | Slowly Changing Dimension Type 2。属性が変わったとき既存行を上書きせず、新しい行を挿入して有効期間（`valid_from` / `valid_to`）で区別するデータモデリング手法。過去の任意時点の状態を復元できる |
| **指数バックオフ** | 再試行のたびに待機時間を倍々に伸ばす手法。相手サーバーが不調なときに追い打ちをかけないための措置 |
| **ジッタ** | 待機時間や実行時刻に加えるランダムな揺らぎ。複数のクライアントが同時刻に一斉アクセスすることを防ぐ |
| **半開区間** | `valid_from <= t < valid_to` の形の区間。境界で重複が生じないため、履歴テーブルの標準的な規約 |
| **DOM（Days on Market）** | 募集開始から掲載終了までの日数。市場の流動性の指標 |
| **outbox パターン** | 送信すべきメッセージを一旦 DB のテーブルに書き、別プロセスが取り出して送る方式。送信失敗時の再試行と重複防止が容易になる |

---

## 2. コンプライアンスおよび法的リスク管理

技術選定より先にこの章を置く。この優先順位は素材レポートから引き継ぐ数少ない構成上の判断であり、正しい。

### 2.1 法的リスク管理表

| 関連法規・規範 | リスクの性質 | 本システムの対策 | 実装上の強制点 |
|---|---|---|---|
| **著作権法 第30条の4** | 複製権の侵害 | 事実データ（家賃・面積・間取り・所在地・築年数・募集状況）のみを抽出する。物件画像の保存・再公開、独創的な説明文の転載を行わない | パーサの抽出対象フィールドをホワイトリストで限定（第3.6節） |
| **著作権法 第30条の4 但書** | 「権利者の利益を不当に害する場合」への該当 | **市場分析をスコープ外とし**（第1.3節）、収集データを第三者に提供・公開・販売しない。運用者本人が転居先を探す目的に限定する（第2.5節） | 出力先は Discord Webhook（本人のチャンネル）とローカル DB のみ |
| **刑法 第233条ほか（業務妨害）** | サーバー過負荷による業務妨害 | レート制限・指数バックオフ・同時実行数1・実行あたり上限リクエスト数を、呼び出し側が迂回できないクライアント層で強制する | 第3.3節。HTTP クライアントを直接使わせず、レート制御を内蔵したラッパ経由に限定 |
| **不正アクセス禁止法** | 認証・技術的制限の不正な回避 | ログイン必須領域にアクセスしない。CAPTCHA・WAF・レート制限の迂回を行わない。フィンガープリント偽装・住宅用プロキシによる IP ローテーションを**採用しない** | 第2.4節（禁止事項リスト） |
| **個人情報保護法（APPI）** | 個人データの不正取得 | 物件という「モノ」のデータに限定する。**加えて、本システム自身が保持する利用者側の個人データも最小化する**（第2.6節） | 希望条件テーブルに氏名・メールアドレス・ユーザーIDを持たない |
| **robots.txt（明示的な意思表示）** | 運営者の明示的な拒否表明への違反 | 全リクエスト前に `robots.txt` の `Disallow` 判定を必ず通す。取得失敗時はクロールを実行しない | 第3.2節 |
| **利用規約（民事上の契約）** | 損害賠償、IPブロック | **対象サイトごとに**規約を実装着手前に確認し、確認結果を本文書に記録する。**未確認の状態で「禁止されている／されていない」と断定しない** | 第8章 未確定事項 U-1（UR）・U-7（JKK） |

### 2.2 robots.txt — 実測結果

`https://www.ur-net.go.jp/robots.txt` を実測した（2026-09-05 時点）。

```
User-agent: *
Disallow: /chintai/*/result/?skcs=
Disallow: /chintai/*/result/?line=
Disallow: /chintai/*/result/?station=
Disallow: /chintai/*/result/?station_nm=
Sitemap: https://www.ur-net.go.jp/chintai/sitemap.xml
```

**Disallow されているのは、エリアコード・沿線・駅による賃貸検索結果ページである。**素朴に実装したクローラーが真っ先に叩く場所そのものだ。

一方、以下は Disallow されていない。

- エリアページ `/chintai/kanto/tokyo/area/{code}.html`
- 団地個票 `/chintai/kanto/tokyo/{id}.html`、`/chintai/kanto/tokyo/{id}_report.html`
- サイトマップ `/chintai/sitemap.xml`（明示的に Allow として提示されている）

したがって **本システムは、クエリパラメータ付きの検索結果 URL を一切組み立てない。**エリアページを起点として団地個票を巡回する経路のみを使う（第3.4節）。この制約は「行儀の良さ」ではなく、robots.txt という機械可読な意思表示への適合として設計に組み込む。

#### JKK東京 — robots.txt は両ホストとも不在

**robots.txt はホスト単位で存在する。**JKK東京はホストが2つに分かれているため、両方を実測した（2026-09-05）。

| ホスト | 役割 | robots.txt |
|---|---|---|
| `www.to-kousya.or.jp` | 案内サイト（賃貸住宅情報のランディング） | **HTTP 404** |
| `jhomes.to-kousya.or.jp` | **空室検索システム（JKKねっと）— 実際に空室データを配信するホスト** | **HTTP 404** |

> **重要な解釈:** **404 は「禁止」でも「自由」でもない。**RFC 9309 §2.3.1.3 上は「制限なし」だが（第3.2節）、それは **robots.txt という一つの判断材料が存在しないこと**を意味するにすぎず、第2.1節の残り6つの判断材料はそのまま適用される。**robots.txt が無いサイトでは、利用規約の重みが相対的に増す**（U-7）。なお 5xx やネットワークエラーは逆に「完全 Disallow とみなす」ことが要求される（第3.2節）。

**UR との決定的な差:** UR では「robots.txt が Disallow していない経路だけを使う」という**外部から与えられた客観的な判断基準**が使えた。JKK にはそれが無い。したがって **JKK では同等の制約を自主的に設定する必要がある**（第3.4節）。robots.txt が無いことは、設計上むしろ**慎重さを増す**理由である。

### 2.3 岡崎市立中央図書館事件（Librahack 事件）から取るべき教訓

素材レポートはこの事件を「高頻度なアクセス」が原因と要約していたが、レート設計に必要な事実が落ちている。正確には次の2点が要点である。

1. **問題となったアクセスは約1秒に1リクエストであった。**これは一般的な感覚では「常識的な間隔」であり、それでも逮捕に至った。
2. **障害の根本原因は図書館側システムの欠陥**（セッションが解放されない実装上の不具合）であった。すなわち、**こちらが「常識的」と考える頻度でも、相手側の実装次第で障害を引き起こしうる。**

この2点から導かれる設計上の帰結:

- **1秒に1リクエストは安全域ではない。**本システムはこれより1桁遅い水準を既定とする（第3.3節）。
- **相手が壊れたときに、こちらが加速してはならない。**エラー応答は「再試行すべき一時的失敗」ではなく「即座に手を引くべき信号」として扱う（第3.5節）。

#### この事件が JKK東京に対して特に重い理由

岡崎事件で障害を起こした図書館システムは、**リクエストごとにセッションを確立し、それが適切に解放されない**という実装上の欠陥を持っていた。1秒1リクエストという穏当な頻度でも、解放されないセッションが累積してシステムが停止した。

JKK東京の空室検索の入口 URL は次の形をしている（2026-09-05 実測）。

```
https://jhomes.to-kousya.or.jp/search/jkknet/service/akiyaJyoukenStartInit
```

`StartInit` という命名と `/service/` のパス構成は、**セッションを確立してから条件を引き継いでいく、サーバー側状態を持つ検索システム**であることを強く示唆する。これは岡崎事件の図書館システムと**同じ系統のアーキテクチャ**である。

したがって JKK に対しては次を設計上の要件とする。

1. **UR と同じレート値をそのまま流用しない。**JKK は初期値をより保守的に置き、実測で問題がないことを確認してから緩める（第3.3節）。
2. **セッションを張りっぱなしにしない。**1回のクロール実行で確立するセッション数を最小化し、実行終了時に明示的に破棄する。
3. **エラー率の監視を UR より厳しくする。**応答遅延の増大は、こちらが相手に負荷をかけている兆候として扱い、即座に中断する（第3.5節）。

**これは机上の懸念ではない。**岡崎事件は、悪意も DoS の意図もない個人開発者が逮捕された事例である（第2.3節冒頭）。相手側システムの実装をこちらは検証できない以上、**アーキテクチャの類似は保守側に倒す根拠として十分**である。

### 2.4 禁止事項（実装レベルで採用しない技術）

以下は、素材レポート `:48-69`「ボット検知の回避とステルス実装の技術的考察」に記載されていたが、**本システムでは採用しない**。当該節は本設計書に引き継がない。

| 禁止する技術 | 理由 |
|---|---|
| `playwright-stealth` 等によるフィンガープリント偽装（`navigator.webdriver` の上書き、プラグイン情報の捏造、WebGL 応答の偽装） | 自動化ツールであることを隠す行為であり、サイト側の検知措置の迂回にあたる |
| 住宅用プロキシ（Residential Proxy）による IP ローテーション | **レート制限はサイト管理者が設置した技術的措置そのものであり、その回避は法的リスク管理表「不正アクセス禁止法」行およびハードルール2 に直接違反する** |
| マウス移動・タイピング遅延・スクロールの人間らしい模倣 | 上記と同じ。行動的生体認証の迂回にあたる |
| CAPTCHA の自動突破・回避 | 同上 |
| ログイン領域（マイページ・会員機能）へのアクセス | 不正アクセス禁止法に抵触しうる。**JKK東京では具体的に `jhomes.to-kousya.or.jp/search/jkknet/service/mypageMenu`（JKKねっと マイページ）、ユーザ登録、住宅の申込に関する一切の経路**を対象外とする。アカウントを作成しない |
| ブラウザを装う User-Agent の設定 | 自作クローラーであることを隠す行為。第3.3節で正直な UA を定める |

**ブロックは回避対象ではなく停止信号として扱う。** 403 / CAPTCHA ページ / WAF のブロック画面を受け取った場合、リトライも迂回も行わず即座にジョブを異常終了し、運用者に通知する（第3.5節）。

### 2.5 収集データの利用範囲

**収集データを第三者に提供・公開・販売しない。**運用者本人が転居先を探すためだけに使う。

これは著作権法第30条の4 の但書「著作権者の利益を不当に害することとなる場合」への該当を避けるための判断である。素材レポートは `:21` で「有償で提供されているデータベースの代替となる市場を侵食する利用形態は避ける」と書きながら、`:131` では「不動産投資家や市場アナリストに対する中長期的な価値」を提供すると述べており、内部矛盾していた。**本設計書は市場分析自体をスコープ外とし**（第1.3節）、この矛盾を構造的に解消する。

不動産市場データを扱う有償サービスが現に存在する以上、「個人の転居先探し」と「市場データの提供」の距離は、法的評価において決定的である。前者にとどまる限り但書の射程からは遠い。

将来この方針を変える場合、**設計書の改訂ではなく、法的評価のやり直しから始める。**

### 2.6 利用者自身の個人データ

素材レポートの法的リスク管理表は「収集対象を物件というモノに限定する」ことしか述べておらず、**本システムが自分の利用者について保持するデータ**が検討から漏れていた。

方針:

- 希望条件テーブル（`watch_criteria`）に、氏名・メールアドレス・SNS ユーザーID を持たない。保持するのは検索条件（エリア・家賃上限・間取り等）のみ。
- 通知先は Discord Webhook URL 1本であり、これは DB ではなく Kubernetes Secret に置く（第6.5節）。
- 利用者が本人のみである前提を、本文書に明記された制約とする。他者に使わせる場合は、利用目的の特定・安全管理措置・保存期間の設計を追加する。

### 2.7 シークレット管理（ハードルール7）

素材レポートにはシークレット管理の記述が皆無だった。

| シークレット | 保管場所 | 注入方法 |
|---|---|---|
| Discord Webhook URL | Kubernetes Secret `estate-notifier-secrets` | 環境変数 `DISCORD_WEBHOOK_URL` |
| PostgreSQL 接続情報 | Kubernetes Secret `estate-db-credentials` | 環境変数 `PGPASSWORD` 等 |
| 運用者連絡先（User-Agent 用） | Kubernetes Secret `estate-scraper-secrets` | 環境変数 `CRAWLER_CONTACT` |

- ソースコード・マニフェスト・`.env` のコミットにシークレットを含めない。
- **ログ出力時に Webhook URL と接続文字列をマスクする**（ロギングフィルタで正規表現マスクを強制）。Webhook URL は事実上の認証情報であり、漏洩すれば第三者がチャンネルに投稿できる。
- Secret の実体はクラスタ外の管理（`sops` + age、または手動 `kubectl create secret`）とし、リポジトリには置かない。

---

## 3. クロール設計

### 3.1 全体構成

**対象サイトごとに独立した CronJob を持つ。**1つのジョブで両サイトを巡回しない。理由は3つ。

1. 一方のサイトの障害が他方の収集を巻き込まない（`crawl_run` の健全性判定もサイト単位で独立する）。
2. サイトごとに異なるレート値・スケジュールを設定できる（JKK はより保守的にする — 第2.3節）。
3. Raspberry Pi のメモリ制約上、Chromium を2つ同時に立ち上げたくない。**スケジュールをずらすことで同時実行を避ける**（第6.3節）。

```
Kubernetes CronJob (サイトごと。実行時刻をずらす)
   │
   ├─ 1. サイトアダプタを site_id で解決（'ur' | 'jkk'）
   ├─ 2. 当該サイトが用いる全ホストの robots.txt を取得・判定（第3.2節）
   ├─ 3. crawl_run レコードを status='running' で作成（source_site を記録）
   ├─ 4. 希望エリアから対象ページ URL を導出（アダプタ実装。第3.4節）
   ├─ 5. 一覧ページを開き、物件個票 URL を収集
   ├─ 6. 各個票を巡回し、部屋単位の事実データを抽出
   │      （各リクエストの前に「ホスト単位」の robots.txt 判定 + レート制限を通過）
   ├─ 7. 取得件数の健全性チェック（異常なら valid_to を閉じずに中止）
   ├─ 8. SCD Type 2 差分適用（第4章）
   ├─ 9. 通知対象の抽出 → notification_outbox へ INSERT（第5章）
   └─ 10. crawl_run を status='succeeded' で確定
```

通知の実送信は別 CronJob（`estate-notifier`, 5分毎）が outbox から取り出して行う。クロールジョブが Discord の障害に巻き込まれないようにするため。両サイトの通知は同一の outbox に集約される。

#### サイトアダプタ

サイト固有の知識（巡回経路・セレクタ・エリアコード体系）をアダプタに閉じ込め、レート制限・robots.txt 判定・SCD2 適用・通知は共通実装とする。**アダプタは HTTP クライアントを自前で生成できない**（第3.2節の強制を迂回させないため）。

```python
class SiteAdapter(Protocol):
    site_id: str                      # 'ur' | 'jkk'
    hosts: list[str]                  # このサイトが用いる全ホスト（robots.txt はホスト単位）

    def target_urls(self, areas: list[str]) -> list[str]:
        """希望エリアから巡回対象の一覧ページ URL を導出する。
        検索結果のクエリ URL を組み立ててはならない（第3.4節）。"""

    async def collect_listing_urls(self, client: PoliteHttpClient, page_url: str) -> list[str]: ...
    async def parse_listing(self, client: PoliteHttpClient, url: str) -> list[ListingFact]: ...
```

`ListingFact` は第3.6節のホワイトリストに対応する不変オブジェクトとし、**サイトによらず同一**とする。サイト差は `listing_dim.source_site` と `site_listing_id` で吸収する（第4.3節）。

### 3.2 robots.txt の遵守（実装レベルで強制）

```python
# 概念コード。呼び出し側が迂回できない位置に置く
class PoliteHttpClient:
    def __init__(self, base_url: str, user_agent: str, contact: str) -> None:
        self._robots = self._fetch_robots(base_url)   # 失敗時は例外を送出してジョブを止める
        ...

    async def get(self, url: str) -> Response:
        if not self._robots.can_fetch(self._user_agent, url):
            raise DisallowedByRobotsError(url)        # 握りつぶさない
        await self._rate_limiter.acquire()
        ...
```

| 規則 | 内容 |
|---|---|
| **適用の単位** | **ホスト単位**（スキーム + ホスト + ポート）。「サイト」単位ではない。JKK東京のようにホストが分かれている対象では、**ホストごとに個別に取得・判定する**。あるホストの robots.txt を別ホストに適用してはならない |
| 取得タイミング | ジョブ開始時に、当該サイトが用いる全ホスト分をまとめて取得。プロセス内で24時間キャッシュ |
| 判定の位置 | HTTP クライアントの内部。上位のスクレイパーコードが判定を飛ばせないようにする |
| Disallow に一致したら | 例外を送出しジョブを止める。スキップして続行しない（設計の誤りを隠さないため） |
| パーサ | `protego`（Python）。`urllib.robotparser` はワイルドカードとクエリ文字列の扱いが弱いため使わない |

**`Crawl-delay` が指定されていた場合は、第3.3節の既定値と比較して長い方を採る。**

#### 取得結果ごとの挙動（RFC 9309 準拠）

**404 と 5xx を同一視してはならない。**RFC 9309（Robots Exclusion Protocol）はこの2つを正反対に規定している。

| 取得結果 | RFC 9309 の規定 | 本システムの挙動 |
|---|---|---|
| **200** | ルールを適用する | パースして全リクエストに適用する |
| **4xx（404 等）** = "Unavailable" | §2.3.1.3「crawler **MAY** access any resources on the server」 | robots.txt による制限は無いものとして続行してよい。ただし**その事実を `crawl_run.target_scope` とログに記録**し、当該サイトの利用規約の確認（第8章 U-1 と同等の作業）が済んでいることを前提とする |
| **5xx** = "Unreachable" | §2.3.1.4「crawler **MUST** assume complete disallow」 | **クロールを実行せずジョブを異常終了する** |
| **ネットワークエラー / タイムアウト** | 上記 "Unreachable" と同じ扱い | 同上 |

4xx を「制限なし」として扱うのは仕様上正しいが、**それは robots.txt という一つの判断材料が存在しないことを意味するにすぎない。**残る6つの判断材料（第2.1節の法的リスク管理表）はそのまま適用され、とりわけ利用規約の重みが相対的に増す。

### 3.3 レート制限（具体値）

レート制限は**ホスト単位**で適用する（第3.2節と同じ単位）。値はサイトごとに異なる。

| パラメータ | UR | **JKK東京** | 根拠 |
|---|---|---|---|
| リクエスト間隔 | **3.0〜6.0秒の一様乱数**（平均4.5秒 ≒ 13 req/min） | **6.0〜12.0秒の一様乱数**（平均9秒 ≒ 7 req/min） | UR は岡崎事件の約1秒/req より1桁遅い水準。**JKK はさらに倍の間隔**を初期値とする（第2.3節。セッション型検索システムであり、相手側の実装を検証できないため）。固定間隔にしないのは、規則的なパターンが異常検知を誘発するのと、複数プロセスの同期を避けるため |
| ホストへの同時接続数 | **1** | **1** | 並行アクセスを一切行わない。Playwright のコンテキストも1つに固定 |
| 1実行あたりの最大リクエスト数 | **250** | **150** | 超過したら打ち切って警告。希望エリアの指定ミスによる暴走を止める安全弁 |
| 実行間隔 | **1時間**（`{0-9} * * * *`） | **2時間**（`{30-39} */2 * * *`） | 毎時00分の一斉起動を避けるジッタ付き。**JKK は UR と時刻をずらす**（Raspberry Pi のメモリ競合回避 — 第6.3節） |
| セッションの扱い | — | **1実行あたりのセッション確立を最小化し、終了時に明示的に破棄する** | 第2.3節 |
| **不変条件** | `想定所要時間 = 対象ページ数 × 平均間隔 ≤ 実行間隔 × 0.5` | 同左 | UR: 250 × 4.5秒 ≒ 19分 ≤ 30分。JKK: 150 × 9秒 ≒ 23分 ≤ 60分。**満たさない設定は起動時に検証エラーとする** |
| 実行の重複 | `concurrencyPolicy: Forbid` | 同左 | 前回の実行が終わっていなければ次を起動しない |

> **JKK の値を UR より保守的にした理由（安全側への意図的な傾斜）:** 空室検索の入口が `akiyaJyoukenStartInit` というセッション確立型のエンドポイントであり、岡崎市立中央図書館事件の図書館システムと同系統のアーキテクチャを示唆する（第2.3節）。**相手側の実装品質をこちらは検証できない。**倒れる方向は非対称で、遅すぎれば「物件を取り逃がす」（可逆・UX の劣化のみ）、速すぎれば「業務妨害」（不可逆・刑事リスク）。したがって遅い側に倒す。実測でエラー率と応答時間に問題がないことを確認できてから緩める。

> **クロール頻度の上限に関する原則:** **データ元の更新頻度を超えるクロールは、純粋に無駄な負荷である。** UR のエリアページには「空室は前日以前の状況です」との注記があり、掲載データの更新頻度が1時間より粗い可能性がある。実装着手前に**サイトごとに**更新頻度を実測し（U-4）、過剰と判明した場合は間隔を延ばす。

### 3.4 巡回経路

両サイトに共通する原則を先に置く。

1. **検索結果のクエリ URL を組み立てない。**UR では robots.txt への適合として必須。**JKK では robots.txt が無いため、同等の制約を自主的に課す**（第2.2節）。
2. **エリアコード等のマッピングは静的な設定ファイルとして保持し、実行時に検索 UI を操作して導出しない。**探索的な操作は、意図せずリクエスト数を増やす。
3. **ログイン領域に到達する経路を持たない**（第2.4節）。

#### UR（実測済み）

```
希望エリア設定（例: 東京都 世田谷区・杉並区・中野区）
   ↓ 静的マッピング（市区町村 → UR エリアコード。config/ur_areas.yaml）
エリアページ  /chintai/kanto/tokyo/area/{code}.html
   ↓ Playwright でレンダリング後、団地個票へのリンクを抽出
団地個票      /chintai/kanto/tokyo/{id}.html
   ↓ 部屋単位の募集情報を抽出
部屋レコード
```

**実測済みの事実（2026-09-05）:**
- エリアページの生 HTML には物件データが含まれない。`該当空室件数 ：件`（数値が空）とローディング画像のみで、家賃も空室数も JavaScript 実行後に描画される。→ **Playwright（Chromium）が必要**。
- `sitemap.xml` は約180件の案内ページのみで、**団地個票を含まない**。したがってサイトマップからの列挙はできず、エリアページ経由の導出が必要。
- robots.txt が `/chintai/*/result/?skcs=` 等を Disallow しており、**この経路設計は robots.txt への適合として必須**（第2.2節）。

#### JKK東京（**巡回経路は未確定 — U-8**）

実測できているのは入口までである。

| 判明していること（2026-09-05 実測） | 内容 |
|---|---|
| 案内サイト | `www.to-kousya.or.jp/chintai/index.html`。**物件データを含まない誘導ページ**（住宅名・家賃・間取り・所在地のいずれも生 HTML に無い） |
| 空室検索の入口 | `https://jhomes.to-kousya.or.jp/search/jkknet/service/akiyaJyoukenStartInit`（PC版）／`...akiyaJyoukenInitMobile`（モバイル版） |
| ホスト | 空室データは **`jhomes.to-kousya.or.jp`** が配信する。案内サイトとは別ホスト |
| サイトマップ | `www.to-kousya.or.jp/sitemap.html` に**物件個票への URL は無い**（UR と同様、案内ページのみ） |
| 会員機能 | `.../service/mypageMenu` にログイン領域が存在する。**スコープ外**（第2.4節） |

**未確定（U-8 として第8章に登録）:**

- 空室一覧・物件個票の URL 形式と、それらが静的 HTML か JS レンダリングか
- 物件個票に相当するページが存在するか（検索結果内に全情報が展開される形式かもしれない）
- **セッションを確立せずに一覧を取得する経路が存在するか。**存在するならそちらを優先する（セッションを持たない方が相手システムへの負荷が小さいため — 第2.3節）
- 部屋単位の安定した識別子が取れるか（U-2 の JKK 版）

**この4点が確定するまで JKK のアダプタを実装しない。**推測で `akiyaJyouken*` 系のエンドポイントに対する呼び出しを書くと、探索的なリクエストがそのまま相手サーバーへの負荷になる。調査は**手動のブラウザ操作で行い、自動化しない**（U-8）。

### 3.5 エラー時の挙動

| 応答 | 挙動 |
|---|---|
| 429 / 503 / 5xx | 指数バックオフで再試行。**初期30秒・係数2・最大30分・最大5回・フルジッタ**。上限に達したらジョブを異常終了 |
| **403 / CAPTCHA ページ / WAF ブロック画面** | **再試行しない。即座にジョブを異常終了し、運用者へ通知する**（第2.4節。ブロックは停止信号） |
| タイムアウト・接続エラー | 429 と同じバックオフ。ただし3回連続で同一ホストに失敗したらジョブ中止 |
| パース失敗（セレクタ不一致） | 当該ページをスキップし、生 HTML をアーカイブして継続。ただし失敗率が10%を超えたらジョブを異常終了（サイト改修の可能性が高いため） |

いずれの異常終了でも、**`valid_to` を閉じる処理は実行しない**（第4.5節）。

User-Agent は次の形式とし、自作クローラーであることと連絡先を明示する。ブラウザを装わない。

```
analysis-of-estate/0.1 (personal vacancy watcher; +mailto:${CRAWLER_CONTACT})
```

`CRAWLER_CONTACT` は Kubernetes Secret から注入する。連絡先を公開することで、サイト運営者が無言のブロックではなく連絡という手段を取れるようにする狙いがある（メールアドレスの露出を避けたい場合は U-5 参照）。

### 3.6 抽出対象フィールド（ホワイトリスト）

**列挙されたフィールドのみを抽出する。**パーサは未知のフィールドを自動的に拾わない。

| フィールド | 型 | 分類 |
|---|---|---|
| 団地名 | text | 事実データ |
| 都道府県・市区町村・町名 | text | 事実データ |
| 部屋識別子（棟・号室 or サイト内ID） | text | 事実データ |
| 家賃（月額） | integer（円） | 事実データ |
| 共益費 | integer（円） | 事実データ |
| 間取り | text（`2DK` 等） | 事実データ |
| 専有面積 | numeric（㎡） | 事実データ |
| 階数 | smallint | 事実データ |
| 募集状況 | enum | 事実データ |
| 詳細ページ URL | text | 参照用 |

**抽出しないもの:** 物件画像（保存・再公開ともに行わない）、物件紹介文・キャッチコピー等の記述的テキスト、担当者名・連絡先等の個人に関する情報。

---

## 4. データモデル（SCD Type 2）

> **分析をスコープ外にしても SCD Type 2 を採用する。** 素材レポートは SCD Type 2 の動機を主に「過去の任意時点の再現と投資分析」に置いていたが、それは本設計書ではスコープ外である。それでも SCD Type 2 を採る理由は、**通知の重複排除が状態遷移の履歴を必要とするから**である。
>
> `CLAUDE.md` ハードルール6 は、通知対象を「新規に `is_current_record = TRUE` となった、かつ**直前が募集中でなかった**」物件に限定する。この「直前」を参照するには、直前の版が行として残っていなければならない。上書き（UPSERT）方式では前の状態が消えるため、毎時のクロールで同じ物件を延々と通知し続けることになる。
>
> つまり SCD Type 2 は分析のための贅沢ではなく、**通知機能そのものの前提**である。分析をスコープ外にしても、この章の設計は縮小しない。

### 4.1 素材レポートのスキーマを採用しない理由

素材レポート `:80-84` のサンプルテーブルは、次の理由で SCD Type 2 として成立していなかった（詳細は附録A-4）。

- 同一 `property_id` に `is_current_record = TRUE` が2行存在し、SCD2 唯一の不変条件を自ら破っていた。
- **募集状況を表すカラムが無く**、「募集していない」状態を「行の不在」で表現していた。行が存在しないものの「直前の状態」は SQL で参照できないため、`:99` の通知ロジックが**実装不能**だった。
- ビジネスキーの粒度（`property_id` か `(property_id, room_number)` か）が未定義だった。

本章はこれらを解消した設計を定める。

### 4.2 DBMS の選定

**PostgreSQL 17（拡張なし）。TimescaleDB を採用しない。**

理由は2つあり、2つ目が決定的である。

**(1) 規模が要求しない。** UR の管理戸数は約70万戸、うち同時に募集中の部屋は全国で 10^4 のオーダー。SCD Type 2 は**変更時のみ行を挿入する**ため、仮に全国を対象としても（1部屋あたり年10回の版切りを仮定して）年間約40万行、5年で約200万行にとどまる。

**本設計書は希望エリア限定（第1.3節）なのでこれよりさらに2桁小さい。**例えば東京23区のうち数区に絞れば、同時募集中は 10^2 のオーダー、5年でも 10^4 行台である。素の PostgreSQL が単一の小規模インスタンスで余裕をもって扱う領域であり、ハイパーテーブルのパーティショニングが効いてくる 10^8 行台とは4桁以上の乖離がある。

> 参考: これを SCD2 にせず毎時スナップショットで積むと全国で年3.5億行になる。TimescaleDB が必要になるのはそちらの設計であり、素材レポートは `:73-77` で自らスナップショット方式を退けておきながら `:90` で TimescaleDB を採用しており、判断が噛み合っていなかった。

**(2) TimescaleDB は SCD2 の整合性保証を壊す。** ハイパーテーブル上の UNIQUE インデックスは**すべてのパーティショニングカラムを含まなければならない**（TimescaleDB の仕様）。したがって、本設計の中核である

```sql
CREATE UNIQUE INDEX ... ON listing_history (listing_key) WHERE is_current_record;
```

が**作成できない**。`valid_from` を含めれば作れるが、それでは「現在レコードは高々1件」という制約として無意味になる。ハードルール5・6 の前提を DB レベルで守れなくなるため、採用は害の方が大きい。

行数が 10^8 に近づいた場合は、PostgreSQL 標準の宣言的パーティショニング（`valid_from` によるレンジ分割）で対応する。

### 4.3 スキーマ

```sql
CREATE EXTENSION IF NOT EXISTS btree_gist;

-- ─────────────────────────────────────────────
-- クロール実行記録。実行の健全性判定と欠測期間の特定に必須（誤通知を防ぐため）
-- ─────────────────────────────────────────────
CREATE TYPE crawl_run_status AS ENUM (
    'running', 'succeeded', 'aborted_anomaly', 'failed'
);

CREATE TABLE crawl_run (
    id                 bigserial PRIMARY KEY,
    source_site        text        NOT NULL,
    target_scope       jsonb       NOT NULL,   -- 対象エリア等。どの範囲を観測したかの記録
    started_at         timestamptz NOT NULL,
    finished_at        timestamptz,
    status             crawl_run_status NOT NULL,
    pages_requested    integer     NOT NULL DEFAULT 0,
    listings_observed  integer     NOT NULL DEFAULT 0,
    parse_failures     integer     NOT NULL DEFAULT 0,
    error_summary      text
);

CREATE INDEX crawl_run_site_time ON crawl_run (source_site, started_at DESC);

-- ─────────────────────────────────────────────
-- 部屋の不変属性（ディメンション）
-- ─────────────────────────────────────────────
CREATE TABLE listing_dim (
    listing_key      text        PRIMARY KEY,          -- source_site || ':' || site_listing_id
    source_site      text        NOT NULL,
    site_listing_id  text        NOT NULL,             -- サイト側で安定して一意な識別子（U-2）
    danchi_name      text        NOT NULL,
    prefecture       text        NOT NULL,
    city             text        NOT NULL,
    town             text,
    room_label       text,                             -- '201号室' '3号棟 2DKタイプ' 等
    detail_url       text        NOT NULL,
    first_seen_at    timestamptz NOT NULL,
    UNIQUE (source_site, site_listing_id)
);

-- ─────────────────────────────────────────────
-- 募集状態の履歴（SCD Type 2）
-- ─────────────────────────────────────────────
CREATE TYPE listing_status AS ENUM ('open', 'closed');

CREATE TABLE listing_history (
    id                  bigserial   PRIMARY KEY,
    listing_key         text        NOT NULL REFERENCES listing_dim(listing_key),
    status              listing_status NOT NULL,
    rent_price          integer,                       -- 円/月。status='closed' では NULL
    common_fee          integer,
    layout              text,
    floor_area_sqm      numeric(6,2),
    floor               smallint,
    valid_from          timestamptz NOT NULL,
    valid_to            timestamptz NOT NULL DEFAULT 'infinity',
    is_current_record   boolean     NOT NULL DEFAULT true,
    opened_by_run_id    bigint      NOT NULL REFERENCES crawl_run(id),
    closed_by_run_id    bigint      REFERENCES crawl_run(id),

    CONSTRAINT valid_period          CHECK (valid_from < valid_to),
    CONSTRAINT current_flag_consistent
        CHECK (is_current_record = (valid_to = 'infinity'::timestamptz))
);

-- SCD2 の唯一の不変条件を DB レベルで強制する
CREATE UNIQUE INDEX listing_history_current_uniq
    ON listing_history (listing_key)
    WHERE is_current_record;

-- 区間の重複を DB レベルで禁止する（レースコンディション下でも履歴が壊れない）
ALTER TABLE listing_history ADD CONSTRAINT listing_history_no_overlap
    EXCLUDE USING gist (
        listing_key WITH =,
        tstzrange(valid_from, valid_to) WITH &&
    );

-- AS OF クエリ用
CREATE INDEX listing_history_asof
    ON listing_history (listing_key, valid_from, valid_to);

-- ─────────────────────────────────────────────
-- 消失の追跡（クロール失敗と募集終了を区別するため）
-- ─────────────────────────────────────────────
CREATE TABLE listing_absence (
    listing_key    text        PRIMARY KEY REFERENCES listing_dim(listing_key),
    missing_since  timestamptz NOT NULL,   -- 最初に観測されなくなった実行の開始時刻
    missing_runs   integer     NOT NULL DEFAULT 1
);
```

### 4.4 設計判断の根拠

| 判断 | 内容 | 理由 |
|---|---|---|
| **ビジネスキー** | `listing_key`（部屋単位） | 通知も空室率も部屋単位で意味を持つ。団地単位では「どの部屋が空いたか」を表現できない |
| **`status` カラムを持つ** | `open` / `closed` の enum | 「行の不在」で非募集を表すと、通知クエリ（第5.2節）の「直前の状態」が SQL で参照できなくなる |
| **`valid_to` のセンチネル** | `'infinity'::timestamptz` | `2999-12-31 23:59:59`（素材レポート `:83`）はタイムゾーンが曖昧で、比較演算にも注意が要る。`infinity` は半開区間クエリでも一意インデックスでも自然に振る舞う。`NULL` は比較で罠になるため使わない |
| **タイムゾーン** | 全て `timestamptz`。**UTC で保存し、表示時に JST へ変換** | 素材レポートには TZ の記述が皆無だった。DST は無いが、混在は必ず事故を起こす |
| **`rent_price` の型** | `integer`（円） | 素材レポートは `85,000円` という単位付き文字列だった。数値比較・集計ができない |
| **区間重複の禁止** | `EXCLUDE USING gist` | 一意インデックスだけでは「過去区間が重なる」壊れ方を防げない。同時実行やリトライ時の二重挿入もここで弾かれる |
| **`current_flag_consistent`** | `is_current_record = (valid_to = 'infinity')` | 2つの表現の不整合を構造的に不可能にする |

### 4.5 版を切る条件（変更検知）

**次のカラムの値が変化したときのみ、新しい版を挿入する。**

`status`, `rent_price`, `common_fee`, `layout`, `floor_area_sqm`, `floor`

**設備情報・紹介文・写真枚数等の記述的フィールドを変更検知の対象にしない。**表記ゆれや軽微な文言更新で版が乱発されると、履歴がノイズで埋まり、通知クエリ（第5.2節）が参照する「直前の版」が意味を失うため。

差分適用の手順（1トランザクション内）:

```sql
-- 変化があった行: 旧版を閉じ、新版を挿入する（旧版の削除・属性上書きは行わない）
UPDATE listing_history
   SET valid_to = :run_started_at,
       is_current_record = false,
       closed_by_run_id = :run_id
 WHERE listing_key = :key AND is_current_record;

INSERT INTO listing_history (listing_key, status, rent_price, ..., valid_from, opened_by_run_id)
VALUES (:key, :status, :rent, ..., :run_started_at, :run_id);
```

`valid_from` / `valid_to` には **クロール実行の開始時刻を統一して使う**（各リクエストの時刻ではない）。同一実行内の全レコードで区間の境界を揃えるため。

### 4.6 「募集終了」と「クロール失敗」の区別

**この区別は本システムで最も壊れやすい箇所であり、誤ると数千件の誤通知を生む。**サイト障害・一時ブロック・セレクタ破損で1回のクロールが空データを返すと、全物件の `valid_to` が閉じ、次のクロールで全件が「新規募集」として再検知されるためである。

3段の防御を置く。

**(a) 実行単位の健全性チェック（`valid_to` を閉じる前に判定）**

**判定は `source_site` ごとに独立して行う。**UR の実行が異常でも JKK の履歴に影響を与えない（またその逆）。中央値も同一サイトの実行のみから算出する。

| 条件 | 挙動 |
|---|---|
| `listings_observed` が**同一サイトの**直近3回の成功実行の中央値の **50% 未満** | `status='aborted_anomaly'` で終了。**`valid_to` を一切閉じない**。新規・変更の適用も行わない |
| `parse_failures / pages_requested > 0.10` | 同上 |
| `listings_observed = 0` | 同上（当該サイトの初回実行を除く） |

**(b) 個別物件の消失は連続2回で確定**

消失判定の対象は、**当該実行が実際に巡回した範囲（`crawl_run.target_scope`）に含まれる物件のみ**とする。UR の実行で JKK の物件を「消失」と判定してはならない。

| 観測 | 挙動 |
|---|---|
| 健全な実行で観測された | `listing_absence` から当該行を削除 |
| 健全な実行で観測されなかった（1回目） | `listing_absence` に `missing_since = run.started_at, missing_runs = 1` を UPSERT。**`valid_to` は閉じない** |
| 健全な実行で観測されなかった（2回目以降） | `missing_runs` を加算し、2以上になった時点で `valid_to = missing_since` として版を閉じ、`status='closed'` の新版を挿入 |

`valid_to` に「確定した時刻」ではなく **`missing_since`（最初に消えた時刻）** を使う点が要点。これにより1時間の系統的な遅れが履歴に入らない。

**(c) 欠測の記録**

`crawl_run` に全実行の記録（時刻・件数・成否）を残す。用途は3つ。

1. **(a) の健全性判定の基準値**（直近3回の成功実行の中央値）を算出する。
2. **消失判定を「健全な実行」に限定する。**異常終了した実行を消失のカウントに含めない。
3. **運用の可視化。**自宅 Raspberry Pi は停電・ネットワーク断でクロールが飛ぶことがあり、通知が来ないのが「本当に募集が無い」のか「観測できていない」のかを事後に区別できるようにする。

素材レポートにはこの概念が無かった。

### 4.7 過去時点の再現（AS OF クエリ）

```sql
SELECT d.danchi_name, d.room_label, h.rent_price, h.layout
  FROM listing_history h
  JOIN listing_dim d USING (listing_key)
 WHERE h.valid_from <= :as_of
   AND h.valid_to   >  :as_of      -- 半開区間。境界で重複しない
   AND h.status = 'open';
```

**精度の限界を明記する。** `valid_from` / `valid_to` は事象の発生時刻ではなく**検知時刻**であり、誤差の上限はクロール間隔（1時間）である。素材レポート `:119` の「ミリ秒単位の精度で正確に算出できる」は誤りであり、本設計書はこれを引き継がない。

### 4.8 生データのアーカイブ

各リクエストの応答（レンダリング後 HTML または JSON）を圧縮して保存し、パース失敗時の再処理を可能にする。

- 保存先: PVC 上のファイルシステム。`{source_site}/{run_id}/{sha256}.html.zst`
- 保持期間: **30日**（Raspberry Pi のストレージ容量制約による）
- **物件画像は保存しない**（第2.1節）
- 用途は再パースのみ。再公開・第三者提供は行わない

---

## 5. 通知システム設計

### 5.1 構成（outbox パターン）

```
クロールジョブ  →  notification_outbox に INSERT（同一トランザクション）
                          │
                          ↓
通知ジョブ（5分毎 CronJob） →  Discord Webhook へ送信 → sent_at を記録
```

クロールジョブから Discord を直接叩かない。Discord の障害でクロール結果が失われるのを防ぐためと、再試行を自然に扱えるため。素材レポートは Cloud Tasks / SQS を挙げていたが、単一ノード・単一利用者の規模では DB テーブル1つで足りる。

```sql
CREATE TABLE notification_outbox (
    id           bigserial   PRIMARY KEY,
    history_id   bigint      NOT NULL REFERENCES listing_history(id),
    criteria_id  integer     NOT NULL REFERENCES watch_criteria(id),
    channel      text        NOT NULL,
    payload      jsonb       NOT NULL,
    created_at   timestamptz NOT NULL DEFAULT now(),
    sent_at      timestamptz,
    attempts     integer     NOT NULL DEFAULT 0,
    next_retry_at timestamptz NOT NULL DEFAULT now(),
    last_error   text,

    -- 冪等性: 同一の版・条件・チャネルの組で二重に積まれない
    UNIQUE (history_id, criteria_id, channel)
);

CREATE INDEX notification_outbox_pending
    ON notification_outbox (next_retry_at)
    WHERE sent_at IS NULL;

-- 希望条件（個人識別子を持たない — 第2.6節）
CREATE TABLE watch_criteria (
    id              serial  PRIMARY KEY,
    label           text    NOT NULL,
    source_sites    text[]  NOT NULL,
    cities          text[],
    max_rent        integer,
    min_floor_area  numeric(6,2),
    layouts         text[],
    enabled         boolean NOT NULL DEFAULT true
);
```

### 5.2 通知トリガー（重複排除）

**「新規に `is_current_record = TRUE` となった、かつ直前が募集中でなかった」物件のみ**（`CLAUDE.md` ハードルール6）。

```sql
SELECT h.id, h.listing_key, h.rent_price, h.layout, d.danchi_name, d.detail_url
  FROM listing_history h
  JOIN listing_dim     d USING (listing_key)
 WHERE h.is_current_record
   AND h.status           = 'open'
   AND h.opened_by_run_id = :run_id          -- この実行で新しく挿入された版
   AND NOT EXISTS (                          -- 直前の版が 'open' でない
         SELECT 1
           FROM listing_history p
          WHERE p.listing_key = h.listing_key
            AND p.valid_to    = h.valid_from  -- 半開区間なので直前版は必ずこれで繋がる
            AND p.status      = 'open'
       );
```

直前版が存在しない場合（初回観測）は `NOT EXISTS` が真になり、通知対象となる。これは意図した挙動である。

**家賃改定のみの変化は通知されない。**`status` が `open` → `open` で直前版も `open` のため。値下げ通知が欲しい場合は別条件として明示的に追加する（本設計書のスコープ外）。

### 5.3 Discord Webhook のレート制限と再試行

| 制限 | 値 |
|---|---|
| チャンネル単位 | 5 リクエスト / 5秒（同一チャンネルの全 Webhook で共有） |
| Webhook 単位 | 約 30 リクエスト / 60秒 |
| グローバル | 50 リクエスト / 秒 |

設計:

- **送信間隔を 2.5秒に固定**（= 24 req/min）。Webhook 単位の 30/60s に対して余裕を持たせる。
- `X-RateLimit-Remaining` が 0 のとき、`X-RateLimit-Reset-After` 秒だけ先行して待つ（429 を踏む前に止まる）。
- **429 を受けたら、レスポンスの `retry_after`（秒、float）に従って待つ。**独自のバックオフ計算で上書きしない。
- 5xx / ネットワークエラー: 指数バックオフ（初期10秒・係数2・最大10分・最大6回）で `next_retry_at` を更新。上限到達で `last_error` を残して放置（運用者が確認）。
- **大量検知時のバッチ化:** 1回の実行で通知対象が多数出た場合、**Discord embed を最大10件まとめて1メッセージ**として送る。大規模団地の退去が重なっても送信リクエスト数が線形に増えない。
- **サーキットブレーカー:** 連続5回失敗したら60分間は送信を試みない（`next_retry_at` を一括で先送り）。Discord 側の障害中に無駄な試行を続けない。

### 5.4 通知内容

事実データのみを含める（第3.6節のホワイトリスト範囲）。物件画像を添付しない。紹介文を転載しない。

```
【新規募集】世田谷区 ○○団地
家賃 85,000円 / 共益費 3,500円 / 2DK / 45.2㎡ / 3階
https://www.ur-net.go.jp/chintai/kanto/tokyo/20_XXXX.html
検知 2026-09-05 14:30 JST
```

---

## 6. インフラ設計（Raspberry Pi / Kubernetes）

素材レポート第3章（Google Cloud Run + Cloud Scheduler + Cloud Tasks）は採用しない。実行環境が自宅 Raspberry Pi の Kubernetes クラスタであるため。

### 6.1 前提と制約

| 項目 | 内容 |
|---|---|
| アーキテクチャ | **ARM64（aarch64）**。64bit OS が必須 |
| Kubernetes | k3s 想定（他ディストリビューションでも可） |
| Playwright | ARM64 対応済み。公式 Docker イメージが arm64 で公開されている |
| メモリ | Chromium は **1インスタンスあたり約400MB + ページあたり約120MB**。同時実行数1のため約600MB を見込む |
| ストレージ | **microSD ではなく USB3 SSD / NVMe を使う。** PostgreSQL の書込は microSD の寿命を急速に消耗させ、破損時にデータが失われる |
| 可用性 | 停電・ネットワーク断でクロールが飛ぶ。これは `crawl_run` の欠測として記録され、第4.6節の防御により誤通知にはならない |
| 出口IP | 家庭用回線の IP。ブロックされると世帯全体に影響する。第3.3節のレート設定はこの点でも保守的である必要がある |

### 6.2 コンテナ

ベースイメージは Playwright 公式（`mcr.microsoft.com/playwright/python:v1.5x-jammy`、arm64 対応）。

Chromium 起動フラグ:

```
--disable-dev-shm-usage        # /dev/shm ではなく /tmp を使う（Pi では必須級）
--disable-gpu                  # GPU 無し
--disable-background-networking
--disable-background-timer-throttling
--disable-ipc-flooding-protection
```

**`--single-process` は使わない**（クラッシュ耐性が著しく落ちる）。**`--no-sandbox` は既定で使わない。**サンドボックスを有効にしたまま非 root で動かすため、seccomp プロファイル（`clone` / `setns` / `unshare` のユーザー名前空間操作を許可）を適用する。それが k3s 環境で成立しない場合の代償措置は U-6 に記載。

### 6.3 Kubernetes リソース構成

| 種別 | 名前 | 役割 |
|---|---|---|
| Namespace | `estate` | — |
| StatefulSet | `postgres` | PostgreSQL 17（arm64 公式イメージ）。PVC を SSD 上の StorageClass に配置 |
| CronJob | `scraper-ur` | `{0-9} * * * *`（毎時）。`concurrencyPolicy: Forbid`, `backoffLimit: 0` |
| CronJob | `scraper-jkk` | `{30-39} */2 * * *`（2時間毎、UR と時刻をずらす）。同上 |
| CronJob | `notifier` | `*/5 * * * *`。outbox を排出 |
| CronJob | `pg-backup` | `30 4 * * *`。`pg_dump` を PVC / NAS へ。7世代保持 |
| Secret | `estate-db-credentials` / `estate-notifier-secrets` / `estate-scraper-secrets` | 第2.7節 |
| ConfigMap | `estate-config` | 希望エリア、エリアコードマッピング、レート値 |

**`backoffLimit: 0` とする理由:** 再試行はアプリケーション内の指数バックオフで行う。Kubernetes 側でも再試行すると、対象サーバーに対する試行が二重に膨らむ。異常終了したジョブは次回の CronJob に委ねる（安全側）。

**2つのスクレイパー CronJob のスケジュールを重ねない理由:** Chromium は1インスタンスあたり約400MB＋ページ約120MB を消費する（第6.1節）。UR（毎時 `{0-9}` 分）と JKK（2時間毎 `{30-39}` 分）は**設計上ぶつからない**が、UR 側が想定より長引くと重なりうる。Raspberry Pi のメモリに応じて次のいずれかを採る。

| ノードのメモリ | 方針 |
|---|---|
| 8GB 以上 | 上記スケジュールのまま。万一重なっても limits の合計 3Gi に収まる |
| 4GB | `scraper-*` に共通の排他制御を入れる（PVC を `ReadWriteOnce` で共有してロックとするか、`nodeSelector` + `podAntiAffinity` で同時スケジュールを防ぐ） |

### 6.4 リソース割当

| コンテナ | requests | limits |
|---|---|---|
| `scraper-ur` | cpu 500m / mem 768Mi | cpu 2000m / mem 1536Mi |
| `scraper-jkk` | cpu 500m / mem 768Mi | cpu 2000m / mem 1536Mi |
| `notifier` | cpu 100m / mem 128Mi | cpu 500m / mem 256Mi |
| `postgres` | cpu 250m / mem 512Mi | cpu 1000m / mem 1Gi |

ノード側の設定: zram による圧縮スワップ（物理メモリの 0.5〜1倍）、`vm.swappiness=10`。

### 6.5 セキュリティ

```yaml
automountServiceAccountToken: false     # クラスタ認証情報をスクレイパーに渡さない
securityContext:
  runAsNonRoot: true
  runAsUser: 1000
  allowPrivilegeEscalation: false
  capabilities:
    drop: ["ALL"]
  seccompProfile:
    type: Localhost
    localhostProfile: playwright-seccomp.json
```

外部サイトのコンテンツをレンダリングするコンテナであるため、**クラスタ内の他リソースへアクセスできる資格情報を持たせない。**NetworkPolicy で下り先を対象サイトと PostgreSQL・Discord に限定する。

---


## 7. 採用しない技術と、その理由

素材レポートに記載があったが本設計書で採用しないものを、根拠とともに記録する。実装者が素材レポートを読んで混乱しないようにするため。

| 技術 | 素材レポート | 不採用の理由 |
|---|---|---|
| **TimescaleDB / ハイパーテーブル** | `:90` | 規模が2桁以上足りない。**かつ、ハイパーテーブルでは SCD2 の部分一意インデックス（`UNIQUE (listing_key) WHERE is_current_record`）が張れず、「現在レコードは高々1件」という不変条件を DB レベルで守れなくなる**（第4.2節）。規模以前に、採用は害の方が大きい |
| **SQLite / DuckDB** | — | 希望エリア限定・UR単独では行数が極めて小さく、単一ファイル DB でも容量的には足りる。しかし `EXCLUDE USING gist` による**区間重複の禁止**（第4.3節）が使えず、SCD2 の履歴が壊れる経路を DB レベルで塞げない。整合性制約が本設計の中核であるため PostgreSQL を採る |
| **Parquet / データレイクハウス（S3・GCS）／dbt / Apache Spark / Delta Lake CDF** | `:91` | 市場分析がスコープ外（第1.3節）のため、そもそも分析基盤を必要としない。仮に必要でも、規模が TB 級と3桁乖離しており過剰。Raspberry Pi 上では Spark の実行自体が困難。生データのアーカイブ（第4.8節）で再パース要件は満たす |
| **機械学習・クラスタリングによる優良物件条件の逆算** | `:126` | 市場分析がスコープ外（第1.3節） |
| **稼働空室率・DOM・賃料トレンドの算出** | `:106-128` | 同上。運用者の判断によりスコープアウト。`listing_history` は将来これらを計算できる構造を持つが、**本システムでは実装しない** |
| **`playwright-stealth` 等のステルス実装** | `:48-69` | ハードルール2 に違反（第2.4節） |
| **住宅用プロキシによる IP ローテーション** | `:68` | レート制限という技術的措置の回避そのもの（第2.4節） |
| **Google Cloud Run / AWS Lambda** | `:25-46` | 実行環境が自宅 Raspberry Pi クラスタのため。なお素材レポート `:32` の「Cloud Run のデフォルト600秒」は誤り（実際は300秒） |
| **Cloud Tasks / AWS SQS** | `:104` | 単一ノード・単一利用者の規模では DB の outbox テーブルで足りる（第5.1節） |
| **LINE Messaging API / メール** | `:103` | 通知チャネルは Discord 1本に絞る。チャネルごとにレート特性も認証情報管理も異なり、初版から3種実装するのは過剰。抽象を1つ確立してから追加する |
| **全域クロール** | `:32` | 希望エリア限定の差分クロールとする。リクエスト数が1〜2桁下がり、第3.3節のレートでも1実行が30分以内に収まる。分析をスコープ外にしたことで全域データを必要とする理由が消えた（第1.3節） |
| **SUUMO 等の民間募集ポータル** | `:145`（引用文献9） | ダミー物件の混入によりデータの信頼性が担保できず、かつ利用規約・WAF の水準が公的主体と大きく異なる（第1.3節） |
| **JKK東京の会員機能（JKKねっと）経由でのデータ取得** | — | ログイン領域であり不正アクセス禁止法に抵触しうる。会員登録を行わず、公開されている空室検索のみを対象とする（第2.4節） |

---

## 8. 未確定事項（実装着手前に解決が必要）

**本章が解決するまで `src/` のコード実装に着手しない。**推測で埋めると、それが本システムの法的リスクの根拠になるため。

| ID | 事項 | 内容 | 解決方法 | 暫定方針（安全側） |
|---|---|---|---|---|
| **U-1** | UR の利用規約 | UR 公式サイトに自動収集を禁止する規約条項が存在するかを確認できていない。素材レポート `:22` の「利用規約第4条」は**第三者サービスの規約を誤引用したもの**であり、UR 公式には該当する条文構成が確認できなかった。**「禁止されていない」と結論づけたわけではない**（附録A-1） | 運用者が UR 公式サイトの規約・サイトポリシーを直接確認する。必要なら UR へ照会する | 確認が済むまで、robots.txt 遵守と第3.3節のレート制限を最大限保守的に運用する |
| **U-2** | ビジネスキーの実在性 | `site_listing_id` として何を使うかが未確定。UR は同一団地内の同一間取りを部屋番号なしで「◯◯号棟 △△タイプ」として掲載する場合があり、部屋単位の安定した識別子が取れるか未検証 | 実際の団地個票を数件確認する調査タスク（スパイク） | 詳細ページ URL に含まれるコード + 部屋ラベルのハッシュ。ただし調査で確定させる |
| **U-3** | Playwright の要否 | エリアページが JS レンダリング必須であることは実測済み。**団地個票**が同様かは未確認。個票が静的 HTML なら、個票の巡回は軽量 HTTP クライアントで済み、Raspberry Pi の負荷が大幅に下がる | U-2 と同時に調査 | 全ページを Playwright で処理する（動作確実性を優先） |
| **U-4** | データ更新頻度 | UR のエリアページに「空室は前日以前の状況です」との注記がある。掲載データの更新頻度が1時間より粗い場合、毎時クロールは無駄な負荷 | 数日間、低頻度で観測して更新タイミングを実測する | 実測が済むまで**2時間間隔**で運用し、実測後に第3.3節の値を確定する |
| **U-5** | User-Agent の連絡先 | 連絡先メールアドレスを UA に含めるか。含めればサイト運営者が無言ブロックでなく連絡を選べるが、アドレスがログに残る | 運用者の判断 | 専用のメールアドレスを用意して設定する。用意しない場合は UA から `+mailto:` を省く |
| **U-6** | Chromium サンドボックス | k3s 上で非 root + seccomp によるサンドボックス有効化が成立するかは実機検証が必要。成立しない場合 `--no-sandbox` が必要になる | 実機での検証 | `--no-sandbox` を使う場合の代償措置: `automountServiceAccountToken: false`、`readOnlyRootFilesystem`、NetworkPolicy による下り先制限、クラスタ内他リソースへの到達不可（第6.5節） |
| **U-7** | **JKK東京の利用規約** | サイトポリシー（`www.to-kousya.or.jp/site_p.html`）に自動収集の禁止条項は無く、著作権条項も本設計と衝突しないことを確認済み（附録C-2）。ただし**JKKねっとの会員登録規約（`/mz_kiyaku/touroku.html`）等、別途の規約は本文を確認できていない**。**robots.txt が両ホストとも不在のため、規約の重みが UR より大きい** | 運用者が JKK の規約類を直接確認する。必要なら JKK へ照会する | 確認が済むまで JKK のクロールを開始しない。UR 単独で先行稼働させる |
| **U-8** | **JKK東京の巡回経路** | 空室検索の入口（`jhomes.to-kousya.or.jp/.../akiyaJyoukenStartInit`）までは実測済みだが、一覧・個票の URL 形式、静的 HTML か JS レンダリングか、セッション無しで取得できる経路の有無、部屋単位の識別子が未確定（第3.4節） | **手動のブラウザ操作で調査する。自動化しない**（探索的リクエストがそのまま負荷になるため） | 確定するまで JKK アダプタを実装しない |
| **U-9** | **JKK東京のレート値の妥当性** | 初期値（間隔6〜12秒・1実行150リクエスト・2時間毎）は、セッション型システムに対する保守的な推定であって実測値ではない（第3.3節） | 少数ページで応答時間とエラー率を観測し、余裕があることを確認する | 実測が済むまで初期値を緩めない。**緩める方向の変更は運用者の承認を要する** |

---

## 附録A. 素材レポートからの主な訂正

`gemini_reports_about_estate_scraping.md` に対する `estate-reviewer` のレビュー（`VERDICT: BLOCK`）で指摘された事項のうち、本設計書に影響したもの。

### A-1. UR の利用規約に関する事実主張（CRITICAL）

素材レポート `:22` は「UR賃貸の利用規約第4条においては『本サービスに対するスクレイピング、クローリング等の自動的なデータ収集行為』が明確に禁止されている」と断定していた。

典拠の引用文献11（`:147`）は `https://kuushitsu-info.jp/ur/terms`。この **`空室情報.jp` は「UR都市機構およびJKK東京と一切関係がありません」と自ら明記する第三者の民間サービス**であり、`/ur/terms` はその民間サービス自身の利用規約である。UR の規約ではない。

UR 公式サイトのフッターに「利用規約」は無く、相当するのは `/site/guide.html`（このサイトについて）だが、条文番号構成を採っておらず、スクレイピング禁止条項は確認できなかった。

**なぜ致命的か:** 設計書は SSoT であり「実装はこの内容と乖離してはならない」（`CLAUDE.md`）。ここに「対象サイトが自動収集を明確に禁止している」と書いた上でスクレイピングを実装すれば、**実際には確認されていない禁止条項について、規約違反を認識した上で実行したという書面上の記録を自ら作る**ことになる。

→ 本設計書は当該記述を削除し、U-1 として未確定事項に移した。

### A-2. ステルス実装節（CRITICAL）

素材レポート `:48-69` は `playwright-stealth` による `navigator.webdriver` の上書き・WebGL 偽装、住宅用プロキシによる「**IPベースのレート制限を回避する**」（`:68`）、行動的生体認証の模倣を具体的に記述していた。`:69` に自己抑制的な一文はあるが、**実行側には具体的なパッケージ名・上書き対象・手法が揃い、禁止側は「最低限の偽装にとどめるべきである」という定義なき一文のみ**という非対称があった。

→ 当該節を全面削除し、第2.4節「禁止事項」と第3.5節「ブロックは停止信号」に置き換えた。

### A-3. robots.txt の欠落（CRITICAL）

素材レポート183行中、`robots` という語は **0件**。実在する明示的な拒否表明（第2.2節）を無視した状態で、存在しない規約条項を論じていた。

→ 第2.1節の法的リスク管理表に行を追加し、第3.2節で実装レベルの強制を定めた。

### A-4. SCD2 サンプルスキーマの破綻（HIGH）

素材レポート `:80-84` の検算結果:

- 同一 `property_id`（`UR-101`）に `is_current_record = TRUE` が **2行**。SCD2 唯一の不変条件を自ら破っていた
- 201号室に 2026-03-15 〜 2026-08-01 の行が存在せず、連続的な履歴ではなく「掲載エピソードの区間テーブル」になっていた
- **募集状況カラムが無く**、`:99` の通知ロジック（「直前の状態が空室ではなかった」）が実装不能だった
- `rent_price` が `85,000円` という単位付き文字列。タイムゾーンの指定なし

→ 第4.3節で全面的に再設計した。

### A-5. その他の訂正

| 素材レポート | 記述 | 訂正 |
|---|---|---|
| `:119` | 空室日数を「ミリ秒単位の精度で正確に算出できる」 | **誤り。**`valid_from` / `valid_to` は事象の発生時刻ではなく検知時刻であり、誤差の上限はクロール間隔（第4.7節）。なお本設計書では空室率の算出自体がスコープ外（第1.3節） |
| `:86` | `valid_to` が閉じたことから「募集が終了したことがわかる」 | **わからない。**「そのクロール時点で観測されなかった」ことしか示さない。入居者決定・募集取り下げ・クロール障害が区別できない（第4.6節） |
| `:32` | Cloud Run のデフォルト実行時間は600秒 | **誤り。**公式ドキュメントでは300秒（5分）。最大60分は正しい |
| `:31` | Playwright は Selenium より「リソース効率が約15〜25%優れている」 | 出典は AWS Lambda 上の**実行時間**の個人計測であり、「リソース効率」の一般命題ではない |
| `:15-19` | 法的リスク管理表の5行中4行 | 典拠がスクレイピングツールベンダーのマーケティング記事だった。第2.1節では条文と実測に基づいて再構成した |
| `:44-45` | Playwright の Docker ベストプラクティス | 典拠が公式 `playwright.dev` ではなく、ダイナミックDNS上の非標準ポートで動く出所不明のミラーサイトだった |
| `:22` | 岡崎図書館事件を「高頻度なアクセス」と要約 | 実際は**約1秒に1リクエスト**で逮捕に至り、根本原因は図書館側システムの欠陥だった。この2点がレート設計の要（第2.3節） |
| `:46` | Cloud Run の `--concurrency 1` を並行アクセス制限として記載 | コンテナが受け付ける HTTP リクエスト数であり、**対象サイトへの並行接続数とは無関係**。混同すると「制限済み」と誤認する |
| `:18` | 個人情報保護法対策を「モノのデータに限定」のみ | 本システムが保持する**利用者側の個人データ**が漏れていた（第2.6節） |
| 全体 | シークレット管理 | 記述が皆無だった（第2.7節） |
| 全体 | レート制限の具体値 | 秒数・req/s・並行数の記載が一つも無かった（第3.3節） |
| 全体 | 冪等性・タイムゾーン | 記述が皆無だった（第4.4節・第5.1節） |

---

## 附録B. 参照した一次情報

実測（2026-09-05）:

- `https://www.ur-net.go.jp/robots.txt` — Disallow パターン4件と Sitemap（第2.2節）
- `https://www.ur-net.go.jp/chintai/sitemap.xml` — 約180件の案内ページのみ。団地個票を含まない
- `https://www.ur-net.go.jp/chintai/kanto/tokyo/area/101.html` — 生HTMLに物件データなし。JS レンダリング必須
- `https://www.to-kousya.or.jp/robots.txt` — HTTP 404（robots.txt 不在）
- `https://kuushitsu-info.jp/` — 「UR都市機構およびJKK東京と一切関係がありません」の記載（附録A-1）

公式ドキュメント:

- [Playwright — Docker](https://playwright.dev/python/docs/docker)
- [Google Cloud Run — request timeout](https://docs.cloud.google.com/run/docs/configuring/request-timeout)（附録A-5 の訂正根拠）
- [TimescaleDB — Hypertables and unique indexes](https://www.tigerdata.com/docs/use-timescale/latest/hypertables/hypertables-and-unique-indexes)（第4.2節の決定根拠）
- [Discord — Rate Limits](https://discord.com/developers/docs/topics/rate-limits)（第5.3節）
- [RFC 9309 — Robots Exclusion Protocol](https://www.rfc-editor.org/rfc/rfc9309.html) §2.3.1.3（4xx = Unavailable → MAY access all）／§2.3.1.4（5xx = Unreachable → MUST assume complete disallow）（第3.2節）

JKK東京の実測（2026-09-05、附録C）:

- `https://www.to-kousya.or.jp/robots.txt` — HTTP 404
- `https://jhomes.to-kousya.or.jp/robots.txt` — HTTP 404（空室データの実配信ホスト）
- `https://www.to-kousya.or.jp/site_p.html` — サイトポリシー。自動収集の禁止条項なし、著作権条項あり
- `https://www.to-kousya.or.jp/chintai/index.html` — 物件データを含まない誘導ページ。空室検索の入口 URL を確認
- `https://www.to-kousya.or.jp/sitemap.html` — 物件個票への URL を含まない

参考（規模の概算）:

- [UR賃貸住宅の現状と今後の方向性について（内閣官房）](https://www.kantei.go.jp/jp/singi/gskaigi/kaikaku/wg4/dai2/siryou6.pdf)
- [UR賃貸住宅における空家の状況と活用事例](https://www.jstage.jst.go.jp/article/jares/28/3/28_72/_pdf/-char/ja)

---

## 附録C. JKK東京の調査記録（2026-09-05 時点）

JKK東京（東京都住宅供給公社）を対象に加えるにあたって行った調査の記録。**巡回経路は未確定であり（U-8）、本附録は「入口まで」の調査結果である。**

### C-1. robots.txt — 両ホストとも不在

**JKK東京はホストが2つに分かれている。robots.txt はホスト単位に存在するため、両方を確認した。**

| ホスト | 役割 | robots.txt |
|---|---|---|
| `www.to-kousya.or.jp` | 案内サイト | **HTTP 404** |
| `jhomes.to-kousya.or.jp` | **空室検索システム（JKKねっと）— 空室データの実配信元** | **HTTP 404** |

RFC 9309 §2.3.1.3 上、404 は "Unavailable" であり「crawler MAY access any resources on the server」。すなわち **robots.txt による制限は存在しない**。ただしこれは「robots.txt という判断材料が無い」という意味にすぎず、第2.1節の残り6つの判断材料はそのまま適用される（第3.2節）。

**UR との差:** UR では robots.txt が「どの経路を避けるべきか」を客観的に示してくれた。JKK にはそれが無いため、**同等の制約を自主的に課す**（第3.4節）。

### C-2. サイトポリシー

`https://www.to-kousya.or.jp/site_p.html`（フッターからリンクされる「サイトポリシー」）を確認した。

| 論点 | 結果 |
|---|---|
| スクレイピング・クローリング・自動収集・プログラムによるアクセスの禁止条項 | **無し** |
| 構成 | 1〜8 の節構成。条文番号（第1条…）形式ではない（UR の `/site/guide.html` と同様） |
| 著作権に関する記載 | 「当サイトの内容について、私的使用又は引用等著作権法上認められた行為を除き、当社に無断で転載等を行うことはできません。」／「当サイトの内容の全部または一部について、当社に無断で改変を行うことはできません。」 |

**この著作権条項は本設計書の方針と衝突しない。**理由は3点とも既に設計に組み込まれている。

1. 利用を運用者本人の私的使用に限定している（第2.5節）— 条項自身が「私的使用…を除き」と除外している側に収まる。
2. 転載・再公開を行わない（第2.5節・第3.6節）。
3. 事実データのみを抽出し、物件画像・説明文を複製しない（第3.6節）。

### C-3. サイト構造（入口まで）

| 項目 | 実測結果 |
|---|---|
| 案内サイト | `www.to-kousya.or.jp/chintai/index.html`。**物件データを一切含まない誘導ページ**（住宅名・家賃・間取り・所在地のいずれも生 HTML に無い） |
| 空室検索の入口（PC） | `https://jhomes.to-kousya.or.jp/search/jkknet/service/akiyaJyoukenStartInit` |
| 空室検索の入口（モバイル） | `https://jhomes.to-kousya.or.jp/search/jkknet/service/akiyaJyoukenInitMobile` |
| 地図検索 | `https://www.areamarker.com/jkk-tokyo/map`（**第三者ドメイン**。本システムの対象外とする） |
| 会員機能 | `https://jhomes.to-kousya.or.jp/search/jkknet/service/mypageMenu`。**スコープ外**（第2.4節） |
| 会員登録規約 | `www.to-kousya.or.jp/mz_kiyaku/touroku.html`（存在は確認、**本文は未取得** → U-7） |
| サイトマップ | `www.to-kousya.or.jp/sitemap.html`。**物件個票への URL は無い**（UR と同様） |

### C-4. 岡崎図書館事件との構造的類似（重要）

入口 URL の `akiyaJyoukenStartInit` という命名と `/service/` のパス構成は、**セッションを確立してから検索条件を引き継ぐ、サーバー側状態を持つシステム**であることを強く示唆する。これは岡崎市立中央図書館事件で障害を起こした図書館システムと**同系統のアーキテクチャ**である（第2.3節）。

同事件では、1秒1リクエストという穏当な頻度でも、解放されないセッションの累積によりシステムが停止し、開発者が逮捕された。**相手側の実装品質をこちらは検証できない。**

→ JKK のレート値を UR の約2倍保守的に設定した根拠（第3.3節）。

### C-5. 未確認事項

1. **規約:** 確認したのはサイトポリシー1ページのみ。**JKKねっと会員登録規約（`/mz_kiyaku/touroku.html`）等、別途の規約の本文を取得できていない**（→ U-7）。UR の U-1 と同じ論点だが、**robots.txt が無い分こちらの方が重い。**
2. **巡回経路:** 一覧・個票の URL 形式、静的 HTML か JS レンダリングか、セッション無しで取得できる経路の有無、部屋単位の識別子（→ U-8）。
3. **レート値:** 第3.3節の JKK 側の値は保守的な推定であり実測ではない（→ U-9）。

### C-6. 実装順序（推奨）

**UR を先に完成させ、稼働実績を作ってから JKK に着手する。**理由は3つ。

1. UR は robots.txt・巡回経路・レンダリング方式がすべて実測済みで、未確定事項が少ない。
2. 共通基盤（レート制御・SCD2・通知・健全性チェック）を UR で検証してから、JKK はアダプタの追加だけで済む。
3. **JKK は U-7（規約確認）が未了である。**規約を確認せずにクロールを開始しない。

スキーマ変更は不要である（`listing_dim.source_site` / `watch_criteria.source_sites` が既に複数サイトを前提とした構造になっている）。

---

## 変更履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 0.1 | 2026-09-05 | 初版。`gemini_reports_about_estate_scraping.md` を素材とし、`estate-reviewer` のレビュー（VERDICT: BLOCK）と一次情報の実測に基づいて再構成 |
| 0.2 | 2026-09-05 | **スコープを縮小**（運用者の判断）。(1) 対象を UR 単独とし、JKK東京・都営住宅等の公社系を除外。(2) 民間募集ポータル（SUUMO 等）を、ダミー物件によるデータ信頼性の欠如を理由に明示的にスコープ外と記載。(3) 不動産投資のための市場分析（稼働空室率・DOM・賃料トレンド・機械学習）をスコープアウトし、第7章「分析メトリクス」を削除。これに伴い著作権法第30条の4 但書の懸念が構造的に解消。SCD Type 2 は通知の重複排除に必要なため維持（第4章冒頭に根拠を明記） |
| 0.4 | 2026-09-05 | **JKK東京（東京都住宅供給公社）を対象に追加**（運用者の指示）。主な設計変更: (1) robots.txt とレート制限の適用単位を「サイト」から**「ホスト」**に修正（JKK は `www.to-kousya.or.jp` と `jhomes.to-kousya.or.jp` の2ホスト構成）。(2) サイトアダプタ抽象を第3.1節に定義し、サイトごとに独立した CronJob とする。(3) **JKK のレート値を UR の約2倍保守的に設定** — 空室検索の入口 `akiyaJyoukenStartInit` がセッション確立型であり、岡崎図書館事件のシステムと同系統のアーキテクチャを示唆するため（第2.3節・附録C-4）。(4) JKK の会員機能（JKKねっと マイページ・ユーザ登録・申込）を明示的にスコープ外とし、アカウントを作成しない。(5) 健全性チェック・消失判定を `source_site` ごとに独立させる。(6) **U-7〜U-9 を追加**（JKK の規約確認・巡回経路・レート値の実測）。**U-7 が未了のため JKK のクロールは開始しない。**附録C を全面改訂 |
| 0.3 | 2026-09-05 | **第3.2節の誤りを訂正。**robots.txt の取得失敗を一括して「クロール中止」としていたが、RFC 9309 は 4xx（Unavailable → MAY access all）と 5xx / ネットワークエラー（Unreachable → MUST assume complete disallow）を**正反対に規定**している。取得結果ごとの挙動表を追加。併せて第2.2節に「404 は禁止でも自由でもない」旨の解釈を明記。**附録C を新設**し、JKK東京の調査結果（robots.txt 404、サイトポリシーに自動収集の禁止条項なし、著作権条項は本設計と非衝突、ただし別途の利用規約は未確認）を保存。第1.3節の JKK 除外理由を「法的な理由ではなく対象を1サイトに絞るための判断」と明確化 |
